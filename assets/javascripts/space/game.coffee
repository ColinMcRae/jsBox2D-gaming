angular.module('Space')
  .service 'init', () ->
    console.log 'inited'
#    initDraw()
    canvas = $('canvas')
#    createGround world, canvas.width(), canvas.height()
#    buildLevel()
#    window.setInterval update, 1000 / 60
